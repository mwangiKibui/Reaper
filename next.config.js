/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
}

// module.exports = nextConfig

module.exports = {
  basePath: '',
  images: {
    domains: ['images.unsplash.com'],
  }
}
